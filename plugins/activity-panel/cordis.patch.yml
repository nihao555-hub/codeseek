# 港窑实时：会话顶栏和 Chat / Trajectory 并列的 conversation.view。
# 不装 overlay 浮层，也不装 omdsh-dev/DSH-better-sidebar。
# 必须用包名，client-modules 才读 dsh.client。
- insert:
    - id: codeseek-activity-panel
      name: codeseek-activity-panel
