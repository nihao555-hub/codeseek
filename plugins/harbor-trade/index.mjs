/**
 * 港窑外贸 DSH 插件。
 * 工具仍走官方 @deepseek-ai/dsh-mcp-client（MCP 是 DSH 的插件行）。
 * 本包提供 bundle identity，并把官方 schedule overlay 绑进同一组合包。
 */
export const name = 'codeseek-harbor-trade'

export function apply() {
  return undefined
}
